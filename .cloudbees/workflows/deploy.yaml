apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: deploy
on:
  workflow_dispatch:
jobs:
  deploy:
    steps:
      - uses: cloudbees-io/register-deployed-artifact@v2
        with:
          labels: aspm-ecr/sv2-build
          artifact-id: e7138669-4944-488b-aeaf-8e06852abc6c
          target-environment: Pre-Prod
